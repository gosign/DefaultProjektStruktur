# Use this file in order to specify Typoscript configuration
# which is specific to the current environment, that is, the
# current server the page is running on, such as the baseURL

# config.baseURL = http://CHANGEME
